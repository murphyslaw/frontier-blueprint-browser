export const layout = "layouts/layout.tsx";
export const features = [];

export default function () {
  return (
    <>
      <p class="notice">
        The system you were looking for doesn't exist in the shattered Frontier.
        Did you make the right jump?
      </p>

      <p>
        <a href="/">Self-destruct</a> and spawn at the Network Node.
      </p>
    </>
  );
}
